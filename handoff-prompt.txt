Continuing work on causly-server (repo: D:\causly-server, GitHub: KNIHAL/causly-server).
All current work is on branch `feature/notion-terraform-azure-readme` — NOT yet merged to main.

Session so far added 6 new tool categories (Notion/Terraform/Docker/Postgres-MySQL/Secrets-manager/Sentry,
75 new tools, 181 total), rewrote README (product-lifecycle positioning, MIT license, custom-build CTA),
updated CONTRIBUTING/ROADMAP/CHANGELOG/BUILD_LOG, and built a full Docusaurus docs site (website/ = source,
docs/ = build output that GitHub Pages serves) with a dark dev-console themed homepage + 13 doc pages.

Remaining work, in priority order:

1. Merge `feature/notion-terraform-azure-readme` into `main` (nothing is live until this happens)
2. Enable GitHub Pages in repo settings: source = `main` branch, folder = `/docs`
3. Verify the docs site renders correctly on GitHub Pages once live (baseUrl is
   `/causly-server/`, configured in website/docusaurus.config.js)
4. Logo/favicon — Kumar is adding his own `favicon.ico` and `logo.svg` to
   `website/static/img/` (same filenames, so no code changes needed once added) — rebuild
   with `npm run build:docs` (from website/) after he adds them
5. Untested-but-code-complete tools worth live-verifying when there's a real use case:
   - Terraform: fmt, output, state_mv, state_rm, import, state_pull, graph, plan_comment
   - docker_push (needs real registry auth)
   - Sentry: resolve_issue, ignore_issue, add_comment (test token lacked event:write scope)
6. Azure/GCP/AWS direct-cloud tools are deliberately OUT OF SCOPE (Kumar's call — avoiding
   cloud account cost) — do not add unless Kumar explicitly asks again

Read /areas/causly-server.md and /areas/causly-hosted.md in memory for full context before
starting.
